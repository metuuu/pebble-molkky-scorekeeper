#pragma once

// Pins art — a fake-3D rendering of the 12-pin Mölkky formation as numbered
// cylinders (green outline, no fill). A standalone preview screen for the store
// icon artwork; pushed from Settings. Back pops.
void pins_art_push(void);
